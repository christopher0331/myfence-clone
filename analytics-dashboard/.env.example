# Supabase project that holds the page_events table (same project as myfence.com).
SUPABASE_URL=https://tlsayvwmcqnmdoairbeb.supabase.co

# Service role key (server-only) used to read aggregated analytics. NEVER expose to the client.
SUPABASE_SERVICE_ROLE_KEY=

# Secret required to view the dashboard: visit https://analytics.myfence.com/?key=THIS_VALUE
ANALYTICS_DASHBOARD_SECRET=
